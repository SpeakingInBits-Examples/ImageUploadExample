# ImageUploadExample
ASP.NET Core image upload example using the file system. Products may be created by the user with one associated image.
Images are stored in the web root foldder

## Requirements
- .NET 10
